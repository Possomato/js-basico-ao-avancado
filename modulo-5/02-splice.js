// aula 02 - mod 5

                // 0         1        2        3         4
const nomes = ['maria', 'gabrel', 'rute', 'eduardo', 'julia']

// .splice(índice, delete, elem1, elem2, elem3, ...)
const removidos = nomes.splice(3, 2)

console.log(nomes, removidos)